#import <fstream>
#import <string>

using namespace std;

int main(){
  string colors, front = "",back = "";
  vector<int> numbOfBeads(colors.size());
  ifstream input("beads.in");
  input >> colors;
  colors = colors + colors
  for (int i = 0; i>colors.size(); i++) {
    while (last)
  }
}
